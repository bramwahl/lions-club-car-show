'use client';
import { useEffect,useState } from 'react';
import type { Award } from '../../../src/domain/awards';
import type { statistics } from '../../../src/workflows/types';
export type Results={awards:Award[];stats:ReturnType<typeof statistics>;lions:{number:string|null;vehicle:string;votes:number}[];updated:string};
export function LiveResults({event,historical,initial}:{event:string;historical:boolean;initial:Results}){const [data,setData]=useState(initial),[error,setError]=useState(false);useEffect(()=>{if(historical)return;let stopped=false;const controller=new AbortController();let timer:ReturnType<typeof setTimeout>;const load=async()=>{try{const res=await fetch(`/admin/awards/live?event=${encodeURIComponent(event)}`,{cache:'no-store',signal:controller.signal});if(!res.ok||!res.headers.get('content-type')?.includes('application/json'))throw new Error('Unavailable');const next=await res.json() as Results;if(!stopped){setData(next);setError(false);}}catch{if(!stopped)setError(true);}finally{if(!stopped)timer=setTimeout(load,5000);}};timer=setTimeout(load,5000);return()=>{stopped=true;controller.abort();clearTimeout(timer);};},[event,historical]);
 return <div className="award-results"><p className="no-print" role="status">{historical?'Historical results':error?'Live refresh interrupted. Showing the last successful update.':`Live summary · refreshes every 5 seconds · ${data.stats.judged} judged / ${data.stats.checked} checked in`}</p>{!historical&&<p className="notice no-print">{data.stats.registered>0&&data.stats.judged===data.stats.registered?'All registered cars have Judged status.':'Provisional while judging is in progress.'} Awards preserve the legacy eligibility and tie rules, including existing partial scores.</p>}{data.awards.length===0?<p className="empty">No scores have been submitted for this event.</p>:<>{['Best in Show','Best in Class','Best in Category','Top 40'].map(group=><section key={group} className="award-section"><h2>{group}</h2><div className={group==='Best in Show'?'':'award-grid'}>{data.awards.filter(r=>r.award===group||r.award.startsWith(group+':')).map(r=><article key={`${r.award}-${r.car_id}`} className={group==='Best in Show'?'best-in-show':'award-card'}><div className={group==='Best in Show'?'eyebrow':'award-title'}>{r.award}</div><h3>#{r.car_number_display} · {r.vehicle_year} {r.vehicle_make} {r.vehicle_model}</h3><p>{r.participant_name} · {r.participant_city}</p><div className="award-scoreline"><span>Paint <strong>{r.overall_paint??'—'}</strong></span><span>Interior <strong>{r.overall_interior??'—'}</strong></span><span>Engine <strong>{r.overall_engine??'—'}</strong></span><span>Total <strong>{r.total_score??'—'}</strong></span></div></article>)}</div></section>)}</>}<p className="hint">* Equal category score among remaining eligible cars. No secondary tie-breaker is applied.</p><section className="section-space"><h2>Lions Choice</h2><p>Separate vote totals; these do not enter scoring or award calculations.</p><div className="table-wrap"><table><thead><tr><th>Car #</th><th>Vehicle</th><th>Votes</th></tr></thead><tbody>{data.lions.map(r=><tr key={r.number}><td data-label="Car #">{r.number??'Not checked in'}</td><td data-label="Vehicle">{r.vehicle}</td><td data-label="Votes">{r.votes}</td></tr>)}</tbody></table>{!data.lions.length&&<p className="empty">No votes recorded.</p>}</div></section></div>;
}
